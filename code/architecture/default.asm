#importonce 

.namespace Default {
    .label SCREEN_CHAR_COLUMNS = 40                     // can also be set to 38
    .label SCREEN_CHAR_ROWS = 25                        // can also be set to 24
    .label SCREEN_MEMORY = $0400
    .label IRQ = $ea31
    .label NMI = $fe47
}